--[[ This is only here so it shows up in whichkey ]]
vim.keymap.set('n', '<leader>g', function()
	end,
	{desc = "git-related"}
)

--[[ This is only here so it shows up in whichkey ]]
vim.keymap.set('n', '<leader>f', function()
	end,
	{desc = "find-related"}
)

--[[ This is only here so it shows up in whichkey ]]
vim.keymap.set('n', '<leader>a', function()
	end,
	{desc = "autocomplete"}
)

